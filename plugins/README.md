# Plugins Directory

Place marketplace plugins in this directory as `plugins/<plugin-name>/`.

Each published plugin should:

- have a folder name that matches its marketplace entry name
- include its required Codex plugin files
- be added to `.agents/plugins/marketplace.json` only when it is real and ready
